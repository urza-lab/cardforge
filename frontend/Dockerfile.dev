# Development image: runs the Vite dev server with hot module reload.
# Not used in production (see frontend/Dockerfile for the nginx-based build).
FROM node:22-alpine
WORKDIR /app
EXPOSE 5173
CMD ["sh", "-c", "npm install && npm run dev"]
