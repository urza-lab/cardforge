# Placeholder note: secrets-init reuses the `runtime` stage of backend/Dockerfile
# via docker-compose's `target: runtime`, overriding entrypoint/command. This
# file exists only for discoverability; there is nothing to build here.
